<IfModule mod_rewrite.c>
    RewriteEngine On

    # Si la petición no es un archivo ni un directorio existente, redirigir a index.php
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.php [L]
</IfModule>
