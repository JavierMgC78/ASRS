<IfModule mod_rewrite.c>
    RewriteEngine On
    # Redirigir el tráfico del directorio raíz al subdirectorio public
    RewriteRule ^$ public/ [L]
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule (.*) public/$1 [L]
</IfModule>
